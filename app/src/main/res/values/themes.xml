<resources>
    <!-- Platform theme used before Compose takes over (no Material XML dependency). -->
    <style name="Theme.Shadey" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
