// Root build file. Plugins are declared in the version catalog (gradle/libs.versions.toml)
// and applied in the individual module build files so that :core can be configured
// without resolving the Android Gradle Plugin.
